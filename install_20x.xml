<?xml version="1.0"?>
<!DOCTYPE modification SYSTEM "http://www.simplemachines.org/xml/modification">
<modification xmlns="http://www.simplemachines.org/xml/modification" xmlns:smf="http://www.simplemachines.org/">
<id>Colin:ForgotPasswordEmailOnly</id>
<name>ForgotPasswordEmailOnly</name>
<version>1.0</version>
<file name="$sourcedir/Reminder.php">
    <operation>
        <search position="replace">
    // Find the user!
    $request = $smcFunc['db_query']('', '
        SELECT id_member, real_name, member_name, email_address, is_activated, validation_code, lngfile, openid_uri, secret_question
        FROM {db_prefix}members
        WHERE ' . $where . '
        LIMIT 1',
        array_merge($where_params, array(
        ))
    );
        </search>
        <add />
    </operation>
    <operation>
        <search position="replace">
        $where_params['member_name'] = $_POST['user'];
        </search>
        <add />
    </operation>
    <operation>
        <search position="replace">
    if ($smcFunc['db_num_rows']($request) == 0 && empty($_REQUEST['uid']))
        </search>
        <add><![CDATA[
    if (empty($_REQUEST['uid']))
        ]]></add>
    </operation>
</file>
</modification>
